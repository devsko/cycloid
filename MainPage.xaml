<Page
    x:Class="cycloid.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cycloid="using:cycloid" 
    xmlns:controls="using:cycloid.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <cycloid:ViewModel x:Name="ViewModel" />
    </Page.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ToggleButton Grid.Column="0" Content="Heatmap" IsChecked="{x:Bind Mode=OneWay, Path=ViewModel.HeatmapVisible}" Command="{x:Bind Mode=OneTime, Path=ViewModel.ToggleHeatmapVisibleCommand}" />
        <Grid
            Grid.Column="1">
            <controls:Map />
            <StackPanel
                Orientation="Vertical"
                Spacing="10"
                Margin="10"
                HorizontalAlignment="Left">
                <controls:Compass />
            </StackPanel>
        </Grid>
    </Grid>

</Page>
